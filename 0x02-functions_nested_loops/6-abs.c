#include "main.h"
#include <stdlib.h>

/**
 * _abs - for getting the absolute of a numb
 * @a: arguement for the function
 *
 * Return: always return 0;
 */

int _abs(int a)
{
	abs(a);
	
	return (0);
}
