#include "main.h"
#include <string.h>

/**
 * _strlen - for getting the string length
 *
 * @s : first arguement
 *
 * Return: returns len;
 */


int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
