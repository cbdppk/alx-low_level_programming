#include "main.h"
#include <string.h>

/**
 *_strcpy - for copying a string to the dest
 * @dest: the first arguement
 * @src: second arguement
 *
 * Return: returns a char;
 */

char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
